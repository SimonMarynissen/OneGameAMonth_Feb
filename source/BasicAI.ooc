import AI

BasicAI: class {

	init: func {
		name = "basicAI"
	}
	
	added: func {
		if (entity x < engine width / 2) {
			// entity velocity = + constant
		} else {
			// entity velocity = - constant
		}
	}
}